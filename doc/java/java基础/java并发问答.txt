1，ReentrantLock的底层实现

2，ReentrantLock的公平锁和非公平锁区别
ReentrantLock默认是非公平锁
非公平锁是指在lock()方法加锁时，不先检查AQS队列中是否有线程在排队，而是直接竞争锁。
公平锁是指在加锁时，先检查AQS队列中是否有线程在排队，如果有，则当前线程也进行排队。
不管公平锁还是非公平锁，一旦没竞争到锁，都会进行排队，当锁被释放时，都是唤醒排在最
前面的线程。ReentrantLock是可重入锁。