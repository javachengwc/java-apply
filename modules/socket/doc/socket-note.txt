-------------------------socket笔记---------------------------------------
c/s通信异常
当c-s连接后,s端宕掉,此时c端向s端发送数据,一般不会报错,但当c端从s段读数据,会报Connection reset异常.
当c-s连接后,c端宕掉,此时如果s端从c段读数据,也会报Connection reset异常.
当c-s还没连接,s端没启或宕掉,此时c端连接s端报Connection refused异常.

select和epoll的区别
select 是轮询的方式，epoll 是回调的方式。
比如，有1000个连接，select是一个一个去询问有没有准备好的IO，epoll是当有准备好的IO会主动通知它，
select的时间复杂度是O(n)，epoll的时间复杂度是O(1)，epoll在连接多的情况下更高效。
--------------------------------------------------------------------------